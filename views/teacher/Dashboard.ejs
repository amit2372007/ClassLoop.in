<%- include("../includes/loader.ejs") -%> 
<% layout("/layout/boilerplate2.ejs") -%>


    <head>
        <title>Teacher Educator Portal | ClassLoop.in</title>
        <script id="tailwind-config">
            tailwind.config = {
                darkMode: "class",
                theme: {
                    extend: {
                        colors: {
                            "primary": "#4848e5",
                            "background-light": "#f6f6f8",
                            "background-dark": "#111121",
                        },
                        fontFamily: {
                            "display": ["Inter", "sans-serif"]
                        },
                        borderRadius: {
                            "DEFAULT": "0.5rem",
                            "lg": "1rem",
                            "xl": "1.5rem",
                            "full": "9999px"
                        },
                    },
                },
            }
        </script>
        <style>
            body {
                font-family: 'Inter', sans-serif;
            }

            .material-symbols-outlined {
                font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
            }

            .custom-scrollbar::-webkit-scrollbar {
                width: 6px;
                height: 4px;
            }
            .custom-scrollbar::-webkit-scrollbar-track {
                background: transparent;
            }
            .custom-scrollbar::-webkit-scrollbar-thumb {
                background: #cbd5e1;
                border-radius: 10px;
            }
            .dark .custom-scrollbar::-webkit-scrollbar-thumb {
                background: #334155;
            }

            /* Mobile sidebar animations */
            @keyframes slideIn {
                from {
                    transform: translateX(-100%);
                }
                to {
                    transform: translateX(0);
                }
            }

            @keyframes fadeIn {
                from {
                    opacity: 0;
                }
                to {
                    opacity: 1;
                }
            }

            .sidebar-open {
                animation: slideIn 0.3s ease-out;
            }

            .backdrop-open {
                animation: fadeIn 0.3s ease-out;
            }

            /* Active tab styling */
            .nav-active {
                background: linear-gradient(135deg, #4848e5 0%, #6366f1 100%);
                color: white;
                box-shadow: 0 4px 12px rgba(72, 72, 229, 0.3);
            }

            .nav-active .material-symbols-outlined {
                font-variation-settings: 'FILL' 1, 'wght' 600;
            }
        </style>
    </head>

    <body
        class="bg-background-light dark:bg-background-dark font-display text-slate-900 dark:text-slate-100 min-h-screen">
        <div class="flex h-screen overflow-hidden">

            <%# DESKTOP SIDEBAR — visible on lg+ %>
            <aside
                class="w-64 bg-white dark:bg-background-dark border-r border-slate-200 dark:border-slate-800 flex-col hidden lg:flex flex-shrink-0">
                <div class="p-6 flex items-center gap-3">
                    <a href="/home" class="w-10 h-10 bg-primary rounded-lg flex items-center justify-center text-white">
                        <span class="material-symbols-outlined">school</span>
                    </a>
                    <a href="/home">
                        <h1 class="text-base font-bold leading-none">ClassLoop.in</h1>
                        <p class="text-xs text-slate-500 mt-1 uppercase tracking-wider font-semibold">Teacher Portal</p>
                    </a>
                </div>
                <nav class="flex-1 px-4 py-4 space-y-2 overflow-y-auto custom-scrollbar">
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'Overview' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Overview">
                        <span class="material-symbols-outlined text-[22px]">dashboard</span>
                        <span class="text-sm font-semibold">Overview</span>
                    </a>
                    <a href="/teacher?tab=Attendance"
                        class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'Attendance' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>">
                        <span class="material-symbols-outlined text-[22px]">how_to_reg</span>
                        <span class="text-sm font-medium">Attendance</span>
                    </a>
                    <a href="/teacher?tab=Students"
                        class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'Students' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>">
                        <span class="material-symbols-outlined text-[22px]">group</span>
                        <span class="text-sm font-medium">Students</span>
                    </a>
                    <a href="/teacher?tab=Assignments"
                        class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'Assignments' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>">
                        <span class="material-symbols-outlined text-[22px]">assignment</span>
                        <span class="text-sm font-medium">Assignments</span>
                    </a>
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'Resources' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Resources">
                        <span class="material-symbols-outlined text-[22px]">topic</span>
                        <span class="text-sm font-medium">Resources</span>
                    </a>
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'Timetable' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Timetable">
                        <span class="material-symbols-outlined text-[22px]">calendar_today</span>
                        <span class="text-sm font-medium">Timetable</span>
                    </a>
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'Form' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Form">
                        <span class="material-symbols-outlined text-[22px]">forms_add_on</span>
                        <span class="text-sm font-medium">Form</span>
                    </a>
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'Exam' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Exam">
                        <span class="material-symbols-outlined text-[22px]">quiz</span>
                        <span class="text-sm font-medium">Exam Marking</span>
                    </a>
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'Applications' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Applications">
                        <span class="material-symbols-outlined text-[22px]">description</span>
                        <span class="text-sm font-medium">Applications</span>
                    </a>
                    
                </nav>
                <div class="p-4 border-t border-slate-200 dark:border-slate-800">
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-xl transition-colors <%= activeTab === 'QRCode' ? 'bg-primary/10 text-primary' : 'text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher/QrCode">
                        <span class="material-symbols-outlined text-[22px]">qr_code</span>
                        <span class="text-sm font-medium">Class QR Code</span>
                    </a>
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-xl text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors"
                        href="/home">
                        <span class="material-symbols-outlined text-[22px]">exit_to_app</span>
                        <span class="text-sm font-medium">Exit Teacher Panel</span>
                    </a>
                    <a class="flex items-center gap-3 px-3 py-2.5 rounded-xl text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800 transition-colors"
                        href="/user/logout">
                        <span class="material-symbols-outlined text-[22px]">logout</span>
                        <span class="text-sm font-medium">Logout</span>
                    </a>
                </div>
            </aside>

            <%# MOBILE HAMBURGER BUTTON — fixed top-left %>
            <button 
                id="mobile-menu-btn"
                onclick="toggleMobileSidebar()"
                class="lg:hidden fixed top-4 left-4 z-50 w-12 h-12 bg-primary text-white rounded-xl shadow-lg flex items-center justify-center hover:bg-primary/90 transition-all active:scale-95">
                <span class="material-symbols-outlined text-2xl" id="menu-icon">menu</span>
            </button>

            <%# MOBILE SIDEBAR — slide from left %>
            <aside
                id="mobile-sidebar"
                class="lg:hidden fixed inset-y-0 left-0 w-72 bg-white dark:bg-background-dark border-r border-slate-200 dark:border-slate-800 z-40 transform -translate-x-full transition-transform duration-300 flex flex-col shadow-2xl">
                
                <div class="p-5 flex items-center justify-between border-b border-slate-200 dark:border-slate-800">
                    <div class="flex items-center gap-3">
                        <div class="w-10 h-10 bg-primary rounded-lg flex items-center justify-center text-white">
                            <span class="material-symbols-outlined">school</span>
                        </div>
                        <div>
                            <h1 class="text-base font-bold leading-none">ClassLoop.in</h1>
                            <p class="text-[10px] text-slate-500 mt-1 uppercase tracking-wider font-semibold">Teacher Portal</p>
                        </div>
                    </div>
                    <button onclick="toggleMobileSidebar()" class="p-2 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg transition-colors">
                        <span class="material-symbols-outlined text-slate-600 dark:text-slate-400">close</span>
                    </button>
                </div>

                <nav class="flex-1 px-3 py-4 space-y-1.5 overflow-y-auto custom-scrollbar">
                    <a onclick="closeMobileSidebar()" class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'Overview' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Overview">
                        <span class="material-symbols-outlined text-[22px]">dashboard</span>
                        <span class="text-sm font-semibold">Overview</span>
                    </a>
                    <a onclick="closeMobileSidebar()" href="/teacher?tab=Attendance"
                        class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'Attendance' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>">
                        <span class="material-symbols-outlined text-[22px]">how_to_reg</span>
                        <span class="text-sm font-medium">Attendance</span>
                    </a>
                    <a onclick="closeMobileSidebar()" href="/teacher?tab=Students"
                        class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'Students' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>">
                        <span class="material-symbols-outlined text-[22px]">group</span>
                        <span class="text-sm font-medium">Students</span>
                    </a>
                    <a onclick="closeMobileSidebar()" href="/teacher?tab=Assignments"
                        class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'Assignments' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>">
                        <span class="material-symbols-outlined text-[22px]">assignment</span>
                        <span class="text-sm font-medium">Assignments</span>
                    </a>
                    <a onclick="closeMobileSidebar()" class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'Resources' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Resources">
                        <span class="material-symbols-outlined text-[22px]">topic</span>
                        <span class="text-sm font-medium">Resources</span>
                    </a>
                    <a onclick="closeMobileSidebar()" class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'Timetable' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Timetable">
                        <span class="material-symbols-outlined text-[22px]">calendar_today</span>
                        <span class="text-sm font-medium">Timetable</span>
                    </a>
                    <a onclick="closeMobileSidebar()" class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'Form' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Form">
                        <span class="material-symbols-outlined text-[22px]">forms_add_on</span>
                        <span class="text-sm font-medium">Form</span>
                    </a>
                    <a onclick="closeMobileSidebar()" class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'Exam' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Exam">
                        <span class="material-symbols-outlined text-[22px]">quiz</span>
                        <span class="text-sm font-medium">Exam Marking</span>
                    </a>
                    <a onclick="closeMobileSidebar()" class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'Applications' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher?tab=Applications">
                        <span class="material-symbols-outlined text-[22px]">description</span>
                        <span class="text-sm font-medium">Applications</span>
                    </a>
                </nav>

                <div class="p-3 border-t border-slate-200 dark:border-slate-800 space-y-1.5">
                    <a onclick="closeMobileSidebar()" class="flex items-center gap-3 px-3 py-3 rounded-xl transition-all <%= activeTab === 'QRCode' ? 'nav-active' : 'text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800' %>"
                        href="/teacher/QrCode">
                        <span class="material-symbols-outlined text-[22px]">qr_code</span>
                        <span class="text-sm font-medium">Class QR Code</span>
                    </a>
                    <a onclick="closeMobileSidebar()" class="flex items-center gap-3 px-3 py-3 rounded-xl text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 transition-all"
                        href="/home">
                        <span class="material-symbols-outlined text-[22px]">exit_to_app</span>
                        <span class="text-sm font-medium">Exit Teacher Panel</span>
                    </a>
                    <a onclick="closeMobileSidebar()" class="flex items-center gap-3 px-3 py-3 rounded-xl text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 transition-all"
                        href="/user/logout">
                        <span class="material-symbols-outlined text-[22px]">logout</span>
                        <span class="text-sm font-medium">Logout</span>
                    </a>
                </div>
            </aside>

            <%# BACKDROP — appears when mobile sidebar is open %>
            <div 
                id="mobile-backdrop"
                onclick="closeMobileSidebar()"
                class="lg:hidden fixed inset-0 bg-black/60 backdrop-blur-sm z-30 hidden transition-opacity duration-300"></div>

            <%# MAIN CONTENT AREA %>
            <div id="main-content-area" class="flex-1 overflow-y-auto min-w-0">

                <% if(activeTab==='Overview' ) { %>
                    <%- include("../includes/teacherDashboard/Overview.ejs") %>
                <% } else if(activeTab==='Attendance' ) {%>
                    <%- include("../includes/teacherDashboard/Attendance.ejs") %>
                <% } else if(activeTab==='Students' ){%>
                    <%- include("../includes/teacherDashboard/Students.ejs") %>
                <% } else if(activeTab==='Assignments' ) {%>
                    <%- include("../includes/teacherDashboard/Assignments.ejs") %>
                <% } else if(activeTab==='Form' ) {%>
                    <%- include("../includes/teacherDashboard/Form.ejs") %>
                <% } else if(activeTab==='Exam' ) {%>
                    <%- include("../includes/teacherDashboard/Exam.ejs") %>
                <% } else if(activeTab==='Resources' ) { %>
                    <%- include("../includes/teacherDashboard/Resources.ejs") %>
                <% } else if(activeTab === 'Applications') {  %>
                    <%- include("../includes/teacherDashboard/Applications.ejs") %>
                <% } %>

            </div>
        </div>

        <script>
            function toggleMobileSidebar() {
                const sidebar = document.getElementById('mobile-sidebar');
                const backdrop = document.getElementById('mobile-backdrop');
                const menuIcon = document.getElementById('menu-icon');
                const menuBtn = document.getElementById('mobile-menu-btn');
                
                const isOpen = !sidebar.classList.contains('-translate-x-full');
                
                if (isOpen) {
                    // Close
                    sidebar.classList.add('-translate-x-full');
                    backdrop.classList.add('hidden');
                    menuIcon.textContent = 'menu';
                    menuBtn.classList.remove('bg-slate-900');
                    menuBtn.classList.add('bg-primary');
                } else {
                    // Open
                    sidebar.classList.remove('-translate-x-full');
                    sidebar.classList.add('sidebar-open');
                    backdrop.classList.remove('hidden');
                    backdrop.classList.add('backdrop-open');
                    menuIcon.textContent = 'close';
                    menuBtn.classList.add('bg-slate-900');
                    menuBtn.classList.remove('bg-primary');
                }
            }

            function closeMobileSidebar() {
                const sidebar = document.getElementById('mobile-sidebar');
                const backdrop = document.getElementById('mobile-backdrop');
                const menuIcon = document.getElementById('menu-icon');
                const menuBtn = document.getElementById('mobile-menu-btn');
                
                sidebar.classList.add('-translate-x-full');
                backdrop.classList.add('hidden');
                menuIcon.textContent = 'menu';
                menuBtn.classList.remove('bg-slate-900');
                menuBtn.classList.add('bg-primary');
            }

            // Close sidebar on escape key
            document.addEventListener('keydown', function(e) {
                if (e.key === 'Escape') {
                    closeMobileSidebar();
                }
            });
        </script>

    </body>
    <script src="/js/loader.js"></script>
    </html>
