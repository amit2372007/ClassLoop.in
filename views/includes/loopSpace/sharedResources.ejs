<main class="max-w-[1280px] mx-auto px-3 md:px-6 py-4 md:py-8">
    <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 md:gap-6 mb-6 md:mb-10">
        <div class="flex gap-1 md:gap-2 p-1 bg-slate-100 dark:bg-slate-800/50 rounded-full w-full md:w-fit overflow-x-auto hide-scrollbar">
            <button onclick="filterResources('all')" class="filter-btn px-4 md:px-6 py-1.5 md:py-2 rounded-full text-xs md:text-sm font-semibold bg-primary text-white shadow-md whitespace-nowrap shrink-0">All</button>
            <button onclick="filterResources('Notes')" class="filter-btn px-4 md:px-6 py-1.5 md:py-2 rounded-full text-xs md:text-sm font-semibold text-slate-600 dark:text-slate-400 hover:text-primary dark:hover:text-white transition-colors whitespace-nowrap shrink-0">Notes</button>
            <button onclick="filterResources('PYQs')" class="filter-btn px-4 md:px-6 py-1.5 md:py-2 rounded-full text-xs md:text-sm font-semibold text-slate-600 dark:text-slate-400 hover:text-primary dark:hover:text-white transition-colors whitespace-nowrap shrink-0">PYQs</button>
            <button onclick="filterResources('Video')" class="filter-btn px-4 md:px-6 py-1.5 md:py-2 rounded-full text-xs md:text-sm font-semibold text-slate-600 dark:text-slate-400 hover:text-primary dark:hover:text-white transition-colors whitespace-nowrap shrink-0">Video Lectures</button>
        </div>
        <div class="hidden md:flex items-center gap-4 text-sm text-slate-500 font-medium">
            <span class="material-symbols-outlined text-[20px]">filter_list</span>
            <span>Sort by: <button class="text-primary hover:underline">Recent First</button></span>
        </div>
    </div>

    <style>
        .hide-scrollbar::-webkit-scrollbar {
            display: none;
        }
        .hide-scrollbar {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }
    </style>

    <div class="mb-6 md:mb-8">
        <h2 class="text-xl md:text-2xl text-white font-bold tracking-tight mb-1 md:mb-2">Academic Resources</h2>
        <p class="text-xs md:text-sm text-slate-500">Access high-quality study materials shared for your class.</p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6" id="resource-container">
        
        

        <% if (resources && resources.length > 0) { %>
            <% for (let res of resources) { %>
                <div class="resource-card glass-card bg-white dark:bg-card-dark rounded-xl overflow-hidden flex flex-col group hover:translate-y-[-4px] transition-all duration-300" data-type="<%= res.resourceType %>">
                    
                    <div class="relative h-40 md:h-48 w-full bg-slate-100 dark:bg-slate-900 flex items-center justify-center overflow-hidden">
                        <% if (res.resourceType === 'Video') { %>
                            <div class="absolute inset-0 bg-black/40 group-hover:bg-black/20 transition-all"></div>
                            <span class="material-symbols-outlined text-white text-[50px] md:text-[60px] z-10">play_circle</span>
                        <% } else { %>
                            <div class="absolute inset-0 bg-gradient-to-br from-primary/10 to-transparent"></div>
                            <span class="material-symbols-outlined text-primary text-[60px] md:text-[80px] opacity-20">
                                <%= res.resourceType === 'Notes' ? 'description' : 'link' %>
                            </span>
                        <% } %>

                        <div class="absolute top-3 right-3 md:top-4 md:right-4 bg-white/10 backdrop-blur-md text-white size-9 md:size-10 rounded-full flex items-center justify-center border border-white/20">
                            <span class="material-symbols-outlined text-lg md:text-xl">
                                <%= res.resourceType === 'Video' ? 'play_arrow' : (res.resourceType === 'Notes' ? 'picture_as_pdf' : 'link') %>
                            </span>
                        </div>
                        <div class="absolute bottom-3 left-3 md:bottom-4 md:left-4">
                            <span class="bg-black/60 backdrop-blur-sm text-white text-[9px] md:text-[10px] font-bold px-2 py-1 rounded-md tracking-wider uppercase">
                                <%= res.subject %>
                            </span>
                        </div>
                    </div>

                    <div class="p-4 md:p-6 flex flex-col flex-1">
                        <h3 class="text-base md:text-lg font-bold mb-1.5 md:mb-2 text-white group-hover:text-primary transition-colors line-clamp-1"><%= res.title %></h3>
                        <p class="text-slate-500 dark:text-slate-400 text-xs md:text-sm mb-4 md:mb-6 line-clamp-2"><%= res.description %></p>
                        
                        <div class="mt-auto">
                            <div class="flex items-center justify-between mb-3 md:mb-4 border-t border-slate-200 dark:border-slate-800 pt-3 md:pt-4 gap-2">
                                <div class="flex items-center gap-1.5 md:gap-2 min-w-0 flex-1">
                                    <img src="<%= res.uploadedBy.profilePic.url %>" class="size-5 md:size-6 rounded-full object-cover border border-slate-700 shrink-0" alt="User">
                                    <span class="text-[10px] md:text-xs text-slate-400 truncate">By <span class="text-slate-200"><%= res.uploadedBy.name %></span></span>
                                </div>
                                <span class="text-[10px] md:text-[11px] text-slate-500 shrink-0"><%= res.createdAt.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %></span>
                            </div>

                            <% 
                                // Logic to handle direct download conversion for GDrive
                                let finalLink = res.externalLink;
                                if (finalLink.includes('drive.google.com')) {
                                    finalLink = finalLink.replace('/file/d/', '/uc?export=download&id=').split('/view')[0];
                                }
                            %>

                            <% if (res.resourceType === 'Video') { %>
                                <a href="<%= res.externalLink %>" target="_blank" class="w-full bg-primary py-2.5 md:py-3 rounded-full font-bold text-xs md:text-sm text-white flex items-center justify-center gap-2 hover:brightness-110 transition-all active:scale-95">
                                    <span class="material-symbols-outlined text-base md:text-[18px]">visibility</span>
                                    Watch Video
                                </a>
                            <% } else { %>
                                <a href="<%= finalLink %>" target="_blank" class="w-full bg-primary/20 hover:bg-primary/30 py-2.5 md:py-3 rounded-full font-bold text-xs md:text-sm text-primary flex items-center justify-center gap-2 transition-all border border-primary/30 active:scale-95">
                                    <span class="material-symbols-outlined text-base md:text-[18px]">download</span>
                                    Download <%= res.resourceType %>
                                </a>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% } %>
        <% } %>
    </div>
</main>

<script>
    function filterResources(type) {
        const cards = document.querySelectorAll('.resource-card');
        const buttons = document.querySelectorAll('.filter-btn');

        // Update Button UI
        buttons.forEach(btn => {
            btn.classList.remove('bg-primary', 'text-white', 'shadow-md');
            btn.classList.add('text-slate-600', 'dark:text-slate-400');
        });
        event.currentTarget.classList.add('bg-primary', 'text-white', 'shadow-md');

        // Filter cards
        cards.forEach(card => {
            if (type === 'all' || card.dataset.type === type) {
                card.style.display = 'flex';
            } else {
                card.style.display = 'none';
            }
        });
    }
</script>
